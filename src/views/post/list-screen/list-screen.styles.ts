import { color } from '../../theme'
import { ViewStyle } from 'react-native'

export const rootContainer: ViewStyle = {
  flex: 1,
  backgroundColor: color.background,
  padding: 20,
}

export const plusButton: ViewStyle = {
  position: 'absolute',
  bottom: 20,
  right: 20,
}
